import React from 'react'

// This is just a basic spinner to show while we wait for our data.
// In the future, I'll show you a clever way to build this using a
// higher order component that makes reusing your Loading component
// very easy.

const LoadingSpinner = () => <div>Loading...</div>

export default LoadingSpinner
